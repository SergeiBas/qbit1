using System;

namespace task1
{
    class Task1
    {
        static void Main()
        {
            double result = (0.3 * Math.Pow(5, 2) - 15) / (3.5 + Math.Pow(2, 2));
            Console.WriteLine(result);
        }
    }
}